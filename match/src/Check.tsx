import { ImageList, ImageListItem, ImageListItemBar } from "@mui/material"
import { Link } from "react-router-dom"

export const Check = () => {
    return (
        <main>
            <h1>Welcome Match Page</h1>
            <Link to="/match">リトライ</Link>
            
        </main>
    )
}